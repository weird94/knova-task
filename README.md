# Knova Monorepo

Minimal `pnpm` monorepo for a React + Konva project.

## Workspace

- `apps/web`: Vite React app with `react-konva`
- `packages/canvas-core`: shared canvas types and helpers

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm typecheck
```
