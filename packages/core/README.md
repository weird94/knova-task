# @knova/core

Core package for the Knova editor.

At this stage, the package stores design documents for the layout engine before implementation begins.

## Documents

- `docs/document-model.md`: document model for the editor and layout engine
- `docs/layout-engine.md`: layout engine design for tokenization, measurement, line breaking, paragraph layout, and pagination
